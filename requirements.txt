# --- Core web ---
fastapi==0.116.1
uvicorn[standard]==0.35.0
starlette==0.47.2
pydantic==2.11.7
python-multipart==0.0.20
python-dotenv==1.1.1

# --- MongoDB (Atlas requiere dnspython para mongodb+srv) ---
motor==3.7.1
pymongo==4.14.0
dnspython==2.7.0
certifi==2025.8.3

# Autenticacion SIRE: token_manager.py importa `jose`, y app/core/crypto.py
# cifra las credenciales SUNAT guardadas en Mongo.
python-jose[cryptography]==3.5.0
cryptography==50.0.1

# --- Autenticacion de usuarios (app/routes/auth.py) ---
# passlib usa el backend bcrypt para hashear contrasenas.
passlib==1.7.4
bcrypt==4.3.0
# auth.py aun tipa las dependencias con Session de SQLAlchemy (codigo heredado,
# la persistencia real es Mongo), por lo que el import se ejecuta al arranque.
sqlalchemy==2.0.43

# Imagenes: openpyxl la carga al importar, para incrustar imagenes en Excel.
# 10.1.0 no tiene wheels para Python 3.13 y fallaba al compilar.
pillow==11.3.0

# --- Clientes HTTP (SUNAT / SIRE / Consultas API) ---
httpx==0.28.1
requests==2.32.5
aiohttp==3.12.15
beautifulsoup4==4.13.4

# --- Fechas / zonas horarias ---
pytz==2023.3
python-dateutil==2.8.2

# --- Importación / exportación Excel ---
openpyxl==3.1.5
